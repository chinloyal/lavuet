/*
	Using asynchronous components would decrease the size of
	the compiled app.js file, however because of a bug in
	webpack 4, we are unable to use asynchronous components.
	The bug should be fixed in webpack 5, so until then...
*/

// import ExampleComponent from '@/components/ExampleComponent';

export default {
	// ExampleComponent
};
